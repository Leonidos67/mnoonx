# Copy to client/.env.local for local dev, or set in Vercel → Project → Environment Variables
# No trailing slash. Production: URL of your Express API (Railway, Render, Fly.io, etc.)
REACT_APP_API_URL=http://localhost:5000
